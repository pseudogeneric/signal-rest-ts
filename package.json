{
  "name": "signal-web",
  "version": "0.0.1",
  "description": "ts wrapper around signal-cli-rest-api",
  "license": "MIT",
  "author": "Grant Bacon",
  "type": "commonjs",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@g.l5.ca:2222/srv/git/signal-web.git"
  },
  "main": "dist/index.js",
  "types": "dist/lib.d.ts",
  "scripts": {
    "test": "jest",
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js",
    "types": "tsc --emitDeclarationOnly -d .",
    "clean": "rm -rf dist/"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24.0.0",
    "esbuild": "0.25.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "tslib": "^2.8.1"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "setupFilesAfterEnv": ["<rootDir>/src/service/tests/setup.ts"]
  }
}
